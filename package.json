{
  "name": "intrasync-site",
  "version": "1.0.0",
  "private": true,
  "description": "IntraSync Industrial / CastLogic marketing site",
  "scripts": {
    "build:css": "tailwindcss -i ./src/tailwind.src.css -o ./tailwind.css --minify",
    "watch:css": "tailwindcss -i ./src/tailwind.src.css -o ./tailwind.css --watch"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.0"
  }
}
